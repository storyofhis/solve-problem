#include <iostream>
#include <map>
#include <vector>
#include <set>
#include <cmath>
#include <algorithm>
using namespace std;
typedef long  long int ll;
typedef long double ld;
typedef vector <ll> vl;
int main(int argc, char const *argv[])
{
    ll t;
    cin >> t;
    for (ll i = 0; i < t; i++) {
        ll n, m, x;
        cin >> n >> m >> x;
        
    }
    return 0;
}
